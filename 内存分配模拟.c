#include <stdio.h>
#include <stdlib.h>
#define SIZE 10

typedef struct POINT
{
    int key;
    int next;
} PArray;

void GetDate(PArray A);

int main()
{
    int i,count;
    printf("valid memory is:");
    printf("qeerwrftrdestgsfdy");
    for(i=0;i<SIZE;i++)
    {
        printf("%d",i);
    }
    return 0;
}

void GetDate(PArray A)
{

}
